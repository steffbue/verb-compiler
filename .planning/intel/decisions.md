# Decisions (ADR intel)

No ADR-classified documents were found in this ingest batch
(`CLASSIFICATIONS_DIR` contains only `DOC` and `SPEC` classifications — see
`/Users/steffen/Desktop/Projekte/compiler/.planning/intel/classifications/`).

- source: absent
- status: absent
- decision: absent
- scope: absent

No locked decisions exist to enforce against future ingests. If ADRs are
added in a later ingest batch, this file should gain one `## ADR-{id}: {title}`
entry per decision, per the standard schema:

```
## ADR-{id}: {title}
- source: {path}
- status: locked|proposed
- decision: {statement}
- scope: {scope}
