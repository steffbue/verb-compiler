---
phase: 08-integration-validation-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/superpowers/specs/2026-07-21-arrays-design.md
  - docs/superpowers/plans/2026-07-21-arrays.md
autonomous: true
requirements: [HOUSEKEEP-01]

must_haves:
  truths:
    - "A developer reading docs/superpowers/specs/2026-07-21-arrays-design.md sees the Array value tag stated as 7 (both in the tag table and the value.rs declaration line), matching shipped src/value.rs (TAG_ARRAY: u64 = 7)."
    - "A developer reading docs/superpowers/plans/2026-07-21-arrays.md sees TAG_ARRAY stated as 7 in every place it names the numeric tag value (Architecture line, Global Constraints, Task 1 file list and interfaces, Task 1 value.rs snippet)."
    - "No unresolved tag-collision remains: with arrays at 7 and TAG_MAP at 6, the two docs no longer both claim tag 6 for different types."
  artifacts:
    - "docs/superpowers/specs/2026-07-21-arrays-design.md (corrected, HOUSEKEEP-01)"
    - "docs/superpowers/plans/2026-07-21-arrays.md (corrected, HOUSEKEEP-01)"
  key_links:
    - "The doc value must equal the shipped constant in src/value.rs (TAG_ARRAY=7) and runtime dispatch — the ground truth the correction is validated against."
  assumptions:
    # Lifted from the deterministic edge-probe engine (specless fallback). All 3
    # HOUSEKEEP-01 rows came back `unresolved`; recorded here with planner judgment,
    # never silently dropped. HOUSEKEEP-01 is a doc-string text substitution, not a
    # data operation, so several generic list-edge probes are inapplicable-by-nature.
    - edge: "HOUSEKEEP-01 / adjacency (equal-or-touching values merge, collide, or separate?)"
      probe: "When two things are exactly equal or just touch, do they merge, collide, or separate?"
      resolution: "APPLICABLE and resolved by this fix. The 'collision' is literally the stale tag-6 overlap (arrays-design claims tag 6=Array, maps-design claims tag 6=Map). After correcting Array to 7, tags 6 (Map) and 7 (Array) are adjacent-but-distinct — they separate, no collision. This is the point of HOUSEKEEP-01."
      status: flagged-unverified
    - edge: "HOUSEKEEP-01 / empty (empty, single, or null input?)"
      probe: "What is the result for empty, single-element, or null input?"
      resolution: "Reinterpreted for a text edit: the meaningful boundary is a false-positive substitution. The edit must change ONLY TAG_ARRAY-bound numeric-6 literals; it must NOT touch TAG_MAP=6, unrelated numeric 6s, or the competing maps-design.md. Verified by a scoped grep that TAG_MAP=6 and maps-design.md are unchanged. A plain empty/single/null-input notion does not apply to a static text substitution."
      status: flagged-unverified
    - edge: "HOUSEKEEP-01 / ordering (stable output order when elements compare equal?)"
      probe: "When elements compare equal, is output order specified and stable?"
      resolution: "INAPPLICABLE by nature. A doc text substitution has no collection, no sort, and no output ordering. Recorded and dismissed with rationale rather than fabricating a resolution."
      status: flagged-unverified
  prohibitions:
    # Survivor of the two-stage prohibition-recall pass (specless fallback). Authored
    # descriptor-less (no check wiring) — disposes flagged-unverified, enforced by reviewer.
    - statement: "This is a documentation-only correction. It must NOT modify any shipped code (src/value.rs, runtime/verb.h) and must NOT re-tag or renumber any runtime value tag — altering the shipped TAG_ARRAY/TAG_MAP constants could corrupt runtime value dispatch (D-07)."
      status: unverified
      flagged: true
    - statement: "Must NOT modify docs/superpowers/specs/2026-07-21-maps-design.md — it is read for tag-collision context only, never edited (canonical_refs); its TAG_MAP = 6 is the correct shipped value and stays."
      status: unverified
      flagged: true
---

<objective>
Correct the stale `TAG_ARRAY = 6` literal in the two Arrays design documents to
`TAG_ARRAY = 7`, matching the shipped `src/value.rs` / `runtime/verb.h`
(`TAG_MAP=6`, `TAG_ARRAY=7`), resolving the long-standing doc/code mismatch and
the tag-6 collision with the Maps spec. Satisfies HOUSEKEEP-01 (D-07).

Purpose: A developer reading the Arrays spec/plan must see the tag value that
actually shipped, with no unresolved tag-collision note.
Output: Two corrected Markdown files. Documentation-only — no code changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-integration-validation-release-readiness/08-CONTEXT.md

# Ground truth for the tag values (do NOT edit these):
@src/value.rs
</context>

<artifacts>
## Artifacts this plan produces
- `docs/superpowers/specs/2026-07-21-arrays-design.md` — corrected (tag table row + value.rs declaration line now read 7)
- `docs/superpowers/plans/2026-07-21-arrays.md` — corrected (all numeric TAG_ARRAY occurrences read 7; the "tags 0–5" range text updated to "0–6")
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Correct TAG_ARRAY tag value in arrays-design.md</name>
  <files>docs/superpowers/specs/2026-07-21-arrays-design.md</files>
  <read_first>
    - docs/superpowers/specs/2026-07-21-arrays-design.md (the file being edited — read the current state; the stale literals are the "Value representation" tag table around line 75 and the value.rs declaration sentence around line 77)
    - src/value.rs (ground truth: TAG_MAP is 6, TAG_ARRAY is 7 — the correction target)
    - docs/superpowers/specs/2026-07-21-maps-design.md (read for context ONLY — it correctly states the Map tag is 6; confirm you understand why Array must become 7, do NOT edit this file)
  </read_first>
  <action>
    In docs/superpowers/specs/2026-07-21-arrays-design.md make exactly two numeric
    corrections so the doc matches shipped src/value.rs (TAG_ARRAY is 7):
    (1) In the "Value representation" tag table (the row currently mapping tag number
    six to `Array | ptr to heap {...}`), change the tag number in the leftmost table
    cell from six to seven.
    (2) On the "Add `TAG_ARRAY: u64 = ...` to `src/value.rs`" sentence, change the
    declared value from six to seven so it reads `TAG_ARRAY: u64 = 7`.
    Leave every by-name `TAG_ARRAY` reference (the payload description, the get/set/
    push/pop/len bullets, the codegen/snapshot references) unchanged — those name the
    tag, they do not restate its numeric value. Do NOT touch maps-design.md. Do NOT
    change any shipped code. This is D-07, HOUSEKEEP-01; the parenthetical "Global
    Constraints section" in D-07 under-scoped where the stale literal lives, so correct
    both numeric occurrences in this spec.
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*6' docs/superpowers/specs/2026-07-21-arrays-design.md)" && grep -q 'TAG_ARRAY: u64 = 7' docs/superpowers/specs/2026-07-21-arrays-design.md && grep -qE '^\| 7 \| Array \|' docs/superpowers/specs/2026-07-21-arrays-design.md && grep -q 'TAG_MAP' docs/superpowers/specs/2026-07-21-maps-design.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*6' docs/superpowers/specs/2026-07-21-arrays-design.md` returns NOTHING (no TAG_ARRAY bound to six remains).
    - `grep 'TAG_ARRAY: u64 = 7' docs/superpowers/specs/2026-07-21-arrays-design.md` matches the declaration sentence.
    - The tag table has a row beginning `| 7 | Array |` and no row beginning `| 6 | Array |`.
    - `git diff --stat` shows ONLY docs/superpowers/specs/2026-07-21-arrays-design.md changed by this task; maps-design.md and all files under src/ are untouched.
  </acceptance_criteria>
  <done>arrays-design.md states the Array tag as 7 in both the tag table and the value.rs declaration; no numeric TAG_ARRAY=6 remains; maps-design.md and shipped code unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Correct all TAG_ARRAY numeric occurrences in arrays.md plan</name>
  <files>docs/superpowers/plans/2026-07-21-arrays.md</files>
  <read_first>
    - docs/superpowers/plans/2026-07-21-arrays.md (the file being edited — the numeric `TAG_ARRAY = 6` literals live in: the Architecture paragraph near line 7, the Global Constraints bullet near line 16 which also says "the existing tags 0–5", the Task 1 "Modify: src/value.rs" file-list line near line 28, the Task 1 Interfaces "Produces" line near line 31, and the Task 1 Step 5 Rust snippet near line 82)
    - src/value.rs (ground truth: tags run 0=NIL,1=BOOL,2=INT,3=FLOAT,4=STR,5=CLOSURE,6=MAP,7=ARRAY — so TAG_ARRAY=7 is appended after tags 0–6)
  </read_first>
  <action>
    In docs/superpowers/plans/2026-07-21-arrays.md change EVERY place that states the
    numeric Array tag value from six to seven so the plan matches shipped src/value.rs
    (TAG_ARRAY is 7): the Architecture paragraph ("New value tag `TAG_ARRAY = ...`"),
    the Global Constraints bullet ("`TAG_ARRAY = ...`, appended after the existing tags
    0–5"), the Task 1 "Modify: `src/value.rs` (add `pub const TAG_ARRAY: u64 = ...;`)"
    line, the Task 1 Interfaces "Produces: ... `TAG_ARRAY: u64 = ...` (value.rs)" line,
    and the Task 1 Step 5 Rust snippet line `pub const TAG_ARRAY: u64 = ...;`. In that
    same Global Constraints bullet, also update the phrase "the existing tags 0–5" to
    "the existing tags 0–6", since TAG_MAP=6 now precedes the array tag. Leave the git
    commit-message line and every by-name `TAG_ARRAY` reference in Tasks 2–7 unchanged
    (they name the tag, not its value). Do NOT change any shipped code. This is D-07,
    HOUSEKEEP-01; correcting all numeric occurrences (not just Global Constraints)
    satisfies "the companion plan is corrected to state TAG_ARRAY = 7".
  </action>
  <verify>
    <automated>test -z "$(grep -nE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*6' docs/superpowers/plans/2026-07-21-arrays.md)" && [ "$(grep -cE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*7' docs/superpowers/plans/2026-07-21-arrays.md)" -ge 4 ] && test -z "$(grep -n 'tags 0–5' docs/superpowers/plans/2026-07-21-arrays.md)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*6' docs/superpowers/plans/2026-07-21-arrays.md` returns NOTHING.
    - At least 4 lines now match `TAG_ARRAY ... = 7` (Architecture, Global Constraints, Task 1 file list, Task 1 Interfaces, and the Rust snippet).
    - The phrase "tags 0–5" no longer appears; it reads "tags 0–6".
    - `git diff --stat` shows ONLY docs/superpowers/plans/2026-07-21-arrays.md changed by this task; no src/ file touched.
  </acceptance_criteria>
  <done>Every numeric TAG_ARRAY reference in the plan reads 7; the tag-range text reads "0–6"; no TAG_ARRAY=6 remains; shipped code unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → repo docs | A text find/replace could over-reach beyond the intended TAG_ARRAY literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | arrays-design.md / arrays.md text substitution | low | mitigate | Scope edits to TAG_ARRAY-bound numeric literals only; verify (git diff --stat) that maps-design.md, all src/ files, and unrelated numeric-6 literals are untouched |
| T-08-02 | Information Disclosure (doc/code mismatch) | doc tag value vs shipped src/value.rs | low | mitigate | Cross-check every corrected value against src/value.rs ground truth (TAG_ARRAY=7); grep asserts no residual `=6` binding |

No package-manager installs (npm/pip/cargo add) occur in this plan — the supply-chain checkpoint (T-*-SC) is not applicable.
</threat_model>

<verification>
- `grep -rnE 'TAG_ARRAY[^0-9A-Za-z]*=[^0-9]*6' docs/superpowers/` returns nothing.
- `grep -n 'TAG_MAP' docs/superpowers/specs/2026-07-21-maps-design.md` still present (unchanged).
- `git diff --name-only` lists exactly the two Arrays doc files.
</verification>

<success_criteria>
Both Arrays docs state TAG_ARRAY = 7 everywhere they name the numeric value; no
`TAG_ARRAY = 6` remains anywhere under docs/superpowers/; the Maps spec and all
shipped code are unchanged. HOUSEKEEP-01 satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-integration-validation-release-readiness/08-01-SUMMARY.md` when done.
</output>
